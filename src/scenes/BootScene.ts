import Phaser from 'phaser';

// Load, init, hand off. Empty stub until the boot path is wired in this task.
export class BootScene extends Phaser.Scene {
  constructor() {
    super({ key: 'BootScene' });
  }
}
