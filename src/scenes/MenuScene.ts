import Phaser from 'phaser';

// Hunter / song-line / difficulty / run-length select. Stub for the scaffold task.
export class MenuScene extends Phaser.Scene {
  constructor() {
    super({ key: 'MenuScene' });
  }
}
