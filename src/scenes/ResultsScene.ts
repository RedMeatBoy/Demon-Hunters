import Phaser from 'phaser';

// End-of-run; shows the song-line improvement. Stub for the scaffold task.
export class ResultsScene extends Phaser.Scene {
  constructor() {
    super({ key: 'ResultsScene' });
  }
}
