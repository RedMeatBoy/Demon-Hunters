import Phaser from 'phaser';

// The horde run itself. Empty stub until the boot path is wired in this task.
export class RunScene extends Phaser.Scene {
  constructor() {
    super({ key: 'RunScene' });
  }
}
