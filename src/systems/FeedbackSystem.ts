import Phaser from 'phaser';

// FeedbackSystem — shake, hit-pause, particles, floating text, combo. Stub for the scaffold task.
export class FeedbackSystem {
  constructor(_scene: Phaser.Scene) {}
}
