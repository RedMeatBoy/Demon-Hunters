import Phaser from 'phaser';

// InputSystem — the ONLY place raw keyboard state is read (CLAUDE.md).
// Stub for the scaffold task; real implementation lands in the
// "InputSystem keyboard abstraction + P1 intent" commit.
export class InputSystem {
  constructor(_scene: Phaser.Scene) {}
}
