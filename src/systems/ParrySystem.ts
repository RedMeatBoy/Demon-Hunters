import Phaser from 'phaser';

// ParrySystem — "Hit the Beat": timing windows, Hype meter, signature powers.
// Spec: /specs/HIT-THE-BEAT-SPEC.md. Stub for the scaffold task.
export class ParrySystem {
  constructor(_scene: Phaser.Scene) {}
}
